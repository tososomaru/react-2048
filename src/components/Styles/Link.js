import styled from 'styled-components';

const Link = styled.a`
  color: inherit;
  font-weight: bold;
  font-size: 24px;
  @media (max-width: 480px) {
    font-size: 5vw;
  }
`;

export default Link;
